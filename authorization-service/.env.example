# Must match registration-service's JWT_SECRET — tokens are issued there
# and verified here.
JWT_SECRET=changeme
PORT=8084

CORS_ALLOWED_ORIGINS=http://localhost:5173

LOG_SERVICE_URL=http://localhost:8091
