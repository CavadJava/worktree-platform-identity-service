DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=platform_identity
DB_SSLMODE=disable
JWT_SECRET=changeme-generate-a-real-secret
JWT_TTL_MINUTES=43200
SUPERADMIN_USERNAME=superadmin
SUPERADMIN_PASSWORD=changeme
PORT=8095
CORS_ALLOWED_ORIGINS=http://localhost:5173
LOG_SERVICE_URL=http://localhost:8091
# Only set this to true for a throwaway/dev database with no data worth
# keeping — Migrate() refuses to run its DROP TABLE rebuild against a
# database that already has users unless this is explicitly enabled.
# ALLOW_DESTRUCTIVE_MIGRATE=true
